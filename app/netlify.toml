[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "8"

# Next.js plugin
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Handle Next.js app routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 