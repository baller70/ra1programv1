[build]
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# Handle Next.js routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 